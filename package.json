{
  "name": "@mesto/core",
  "version": "0.0.1",
  "description": "Единый API для проектов, связанных с mesto.co",
  "repository": "github:mesto-co/core.git",
  "homepage": "https://github.com/mesto-co/core#readme",
  "engines": {
    "node": ">=12.18.3"
  },
  "main": "index.js",
  "scripts": {
    "eslint": "[ \"$CI\" = true ] && eslint --quiet -f codeframe --ext js,ts . || eslint --ext js,ts .",
    "eslint:fix": "[ \"$CI\" = true ] && eslint --quiet -f codeframe --ext js,ts . --fix || eslint --ext js,ts . --fix",
    "lint": "npm run eslint && npm run tsc",
    "tsc": "tsc -p .",
    "clean": "rm tsconfig.tsbuildinfo && docker-compose -f ./docker-compose-dev.yml rm --force && rimraf app",
    "test": "npm run lint && jest --testPathPattern=test/",
    "test:ci": "node utils/wait-for.js core 8080 50000 && npm run test",
    "build": "tsc -p .",
    "prestart": "npm run build",
    "start": "node .",
    "start:inspect": "node --inspect=0.0.0.0:9229 .",
    "start:dev": "npm run build && docker-compose -f docker-compose-dev.yml up --force-recreate --build --exit-code-from core",
    "start:in-docker:dev": "knex migrate:latest && knex seed:run && nodemon",
    "start:in-docker:ci": "node utils/wait-for.js postgres 5432 50000 && npm run build && knex migrate:latest && node ."
  },
  "author": {
    "name": "Mesto.Team"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/express": "^4.17.8",
    "@types/jest": "^26.0.13",
    "@types/knex": "^0.16.1",
    "@types/node": "^14.10.1",
    "@types/rimraf": "^3.0.0",
    "@typescript-eslint/eslint-plugin": "^4.0.1",
    "@typescript-eslint/parser": "^4.0.1",
    "eslint": "^7.8.1",
    "eslint-plugin-notice": "^0.9.10",
    "jest": "^26.4.2",
    "nodemon": "^2.0.4",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "ajv": "^6.12.4",
    "aws-sdk": "^2.752.0",
    "debug": "^4.1.1",
    "express": "^4.17.1",
    "knex": "^0.21.5",
    "pg": "^8.3.3",
    "rimraf": "^3.0.2"
  }
}
