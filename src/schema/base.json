[
    {
        "$id": "#RequestBase",
        "type": "object",
        "properties": {
            "X-Request-Id": { "type": "string", "format": "uuid" }
        },
        "required": [ "X-Request-Id" ]
    }
]